import { OnTickCurrentPlayer, TickHeartbeat } from "programming-game";

export function endQuest(heartbeat: TickHeartbeat, player: OnTickCurrentPlayer) {

}

export function maybeAssignEndQuest(heartbeat: TickHeartbeat, player: OnTickCurrentPlayer) {

}
