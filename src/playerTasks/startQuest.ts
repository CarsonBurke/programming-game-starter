import { OnTickCurrentPlayer, TickHeartbeat } from "programming-game";

export function startQuest(heartbeat: TickHeartbeat, player: OnTickCurrentPlayer) {

}

export function maybeAssignStartQuest(heartbeat: TickHeartbeat, player: OnTickCurrentPlayer) {

}
